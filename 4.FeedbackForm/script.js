const formElement =document.forms.feedback;
console.log(formElement.elements)

const {fullname, email , feedback , message , terms} = formElement.elements;


console.log(fullname, email. feddback, message,terms)